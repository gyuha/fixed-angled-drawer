# fg-quick 로그

## 2026-08-18 — 썸스크류 빨강 표시
- 요청: 스크류 색상을 빨강으로, 스샷 재생성
- 결정: 색상은 GUI 속성(ViewObject)이라 헤드리스 pen_holder.py에서는 설정 불가 → screenshot.py에서 이름에 screw가 든 객체에 적용하고, 문서를 저장해 FCStd를 열어도 빨강이 유지되게 함
- 결과: done (thumbscrew·asm_screw 둘 다 적용, PNG 5장 재생성·육안 확인. 첫 렌더에 초록 선택 하이라이트가 찍혀 clearSelection 추가 후 재생성)

## 2026-08-18 — 클램프 아래턱 10mm 축소 + 앞 끝 r5 필렛
- 요청: 책상 체결부가 너무 많이 나와 있음, 표시 부분 10mm 축소 + 잘린 끝 양 모서리에 r5 필렛
- 결정: JAW_REACH 55→45mm(나사 구멍 앞으로 재료 7.4mm 남음). 필렛은 아래턱 앞 끝의 좌우 세로 모서리 2개에 r5, 나사산 절삭 전에 적용해 OCC 실패 회피
- 결과: done (검증 20항목 PASS. STL 실측: 아래턱 y 55~100, 각 코너 정점 0개·r5 원호 58점·앞면 x 10~60으로 필렛 적용 확인)

## 2026-08-18 — 썸스크류 25mm 연장 (얇은 상판 체결 불가 수정)
- 요청: 책상이 얇아 체결이 안 됨, 스크류 높이 2.5cm 증가
- 원인: 샤프트 24mm - 아래턱 14mm = 턱 위 돌출 10mm뿐 → 개구 40mm 대비 30mm 미만 상판에는 나사가 닿지 않음 (README의 "18~25mm 대응"은 틀린 값이었음)
- 결정: THR_LEN 22→47mm, 샤프트를 THR_LEN+2로 파라미터화(24 하드코딩 제거). 같은 결함을 다시 놓치지 않도록 '체결 가능 상판 두께' 검증 항목 추가
- 결과: done (검증 20항목 PASS — 체결 5~40mm, 나사 전체 높이 59mm 실측, 스크린샷 재생성, README 오기재("18~25mm 대응") 정정)

## 2026-08-17 — 뚜껑 분해 뷰 추가
- 요청: 뚜껑이 보이는 스샷으로 README 구성
- 결정: 조립 뷰에서 뚜껑이 '평평한 윗면'으로만 보여 별개 부품임이 안 드러남 → exp_ 접두어 분해 배치(뚜껑을 35mm 띄움) 추가하고 screenshot.py를 접두어 3그룹(parts/asm_/exp_)으로 확장, shot-lid-open.png를 README 마감 절에 배치
- 결과: done (PNG 5장 생성·분해도 육안 확인, README 이미지 5개 참조 무결성 확인, 검증 19항목 PASS)

## 2026-08-17 — 폭 80→70mm 변경 (8cm판은 output-8cm으로 보존)
- 요청: 출력해보니 폭이 넓음. 기존 output을 output-8cm으로 백업하고 폭 7cm 도면 생성
- 결정: output 디렉터리를 통째로 output-8cm으로 이동(사용자의 fixed-drawer.3mf도 8cm 산출물이라 함께 이동), W=70.0으로 바꿔 output에 새로 생성. 다른 파라미터는 W에 종속되거나 폭과 무관해 그대로
- 결과: done (검증 19항목 PASS, STL 6종 폭 70.00mm 실측 확인, 스크린샷 재생성, README 치수/파일구조 갱신)

## 2026-08-17 — 마감 모듈에 천장 일체화
- 요청: 마감 모듈 자체에 뚜껑이 있었으면 함. 지금의 분리형 뚜껑은 2칸 모듈용으로 유지
- 결정: topmodule을 높이 56.7mm(=TOP_H) 한 부품으로 — 상단 3mm 천장 일체, 바닥 개방 유지, 위에 아무것도 안 올라가므로 캐치 창 제거. lid.stl은 그대로 두고 조립 뷰에 module2+lid 예시 추가
- 결과: done (검증 19항목 PASS — 바닥 개방·천장 일체 좌표 확인, 스크린샷 재생성·육안 확인, README/독스트링 갱신)

## 2026-08-17 — 마감용 최상단 모듈 + 평평한 뚜껑
- 요청: 최상단에 쓰는 마감 모듈, 뚜껑 닫았을 때 3칸 모듈의 1칸 높이(56.7mm), 평평한 뚜껑
- 결정: 얕은 트레이(높이 53.7mm, 평바닥·앞턱 램프·개방 전면)+평판 뚜껑(3mm). 뚜껑은 모듈 바닥과 동일한 스냅 스커트를 그대로 써서 어느 모듈 위에도 덮이게 함. 닫은 높이 53.7+3=56.7=H/3
- 후속: 마감 모듈은 바닥 불필요(아래 칸과 연결, 뚜껑이 그 공간을 덮음) → 평바닥·앞턱 제거하고 열린 칼라로 변경, 측벽 FLANGE 레일만 존치
- 결과: done (검증 19항목 PASS — 바닥 개방·레일 존치 확인 포함, 스크린샷 재생성·육안 확인, README/독스트링 갱신)

## 2026-08-17 — README.md 작성
- 요청: README 작성 + 스크린샷 배치
- 결정: output/shot-*.png 4장을 그 자리에서 참조(재생성 시 문서도 자동 최신화). 부품·치수·출력 가이드·조립·파라미터·재생성 절차 수록. pen_holder.py 독스트링의 낡은 산출물 목록도 함께 정정
- 결과: done (이미지 경로 4개 존재 확인, 독스트링 수정 후 재실행 VERIFICATION PASS)

## 2026-08-17 — FCStd 배치 (스크린샷용)
- 요청: FreeCAD 모델에서 스샷 찍을 수 있게 배치
- 결정: 4부품이 원점에 겹쳐 있던 문제 → 부품 나열 뷰(X방향 간격)와 조립 뷰(tier1+module3 적층+썸스크류 체결)를 같은 FCStd에 배치. Placement만 이동하므로 STL은 원점 유지. 추가로 screenshot.py(GUI 실행)로 PNG 4장 자동 생성
- 결과: done (PNG 4장 생성·육안 확인, STL 원점 유지 확인. Gui.showMainWindow는 1.1.3에 없어 제거)

## 2026-08-17 — 앞턱 단차 제거 (서포트 최소화)
- 요청: 앞턱 단차 때문에 서포트가 과도함, 서포트 없을 정도로 최소화
- 결정: 광선투사 분석으로 뒷면-아래 방향 기준 원인 3개 확인 → 앞턱을 수직 단차(6mm)에서 45°+ 자기지지 램프(높이 3mm·기저 8mm)로, 뒷면 스커트 판 제거(측면 스커트가 깊이 위치 결정 담당). 타공 구멍 상단 아치는 Ø4.5 브리징 범위로 판단해 유지
- 결과: done (검증 13항목 PASS·램프 53.5°. 서포트 면적 앞턱 1669→104·2050→104mm², 스커트 882→60mm², 합계 6100→3713 / 5587→3641mm²)

## 2026-08-17 — 측벽 타공 패턴
- 요청: 옆면이 밋밋하지 않게 타공 구멍, 단 옆면의 빈 곳에만
- 결정: Ø4.5·피치 8mm 스태거드 배열, X관통 원기둥으로 양 측벽 동시 타공. 선반 슬래브 띠(±1.5 여유)·캐치 창·상하 결합부·앞뒤 가장자리 회피, 부품별 칸 배치에 맞춰 자동 산출
- 결과: done (검증 12항목 PASS — 2칸 169·3칸 153개, 측벽 투영으로 회피 영역 확인)

## 2026-08-17 — tier1 3칸화 + 모듈 2칸/3칸 변형
- 요청: 바닥 모듈(1단)을 3칸으로, 적층 모듈은 2칸·3칸 두 가지로
- 결정: 칸 수를 make_body 파라미터로. 모든 부품 높이 170 유지(적층 호환), 3칸 피치 56.7mm. 산출물 4종: tier1(3칸)·module2·module3·thumbscrew, 기존 module.stl은 module2.stl로 대체
- 결과: done (검증 11항목 PASS, 단면 분석으로 부품별 선반 수 2/3/3 확인)

## 2026-08-17 — 클램프를 뒷면과 플러시로 (FDM 출력 대응)
- 요청: 연결(클램프) 부위를 뒷면과 맞춰 등을 대고 평평하게 출력되게, 두께 유지·튼튼하게
- 결정: 뒷판을 y 88~100(안쪽)으로 이동, PLATE_UP 보강 제거(칸 침범 방지), 접합 단면 60×12mm로 강성 유지. 책상 물림 깊이는 뒷면에서 12mm 안쪽부터
- 결과: done (검증 10항목 PASS, 단면 점검으로 뒷면 전체 단일 평면 확인)

## 2026-08-17 — 적층 연결부 필렛 제외
- 요청: 위아래 연결 부위(상단 테두리·하단 레일/스커트)에는 필렛 금지, 결합이 자연스럽게
- 결정: z=0 이하 또는 z=170에 닿는 모서리를 필렛 대상에서 제외. 부분 필렛 불가로 측벽 앞 세로 모서리 필렛은 포기, 선반·턱·개구부 필렛만 유지
- 결과: done (검증 10항목 PASS + STL 정점 점검: 연결부 각짐 복원·중간 필렛 유지 확인)

## 2026-08-17 — 앞쪽 모서리 필렛
- 요청: 앞쪽 모서리들에 필렛을 줘서 부드럽게 사용할 수 있게
- 결정: y<벽두께 구간의 모서리 일괄 선택, 반경 1.4mm(벽 3mm 제약) + 실패 시 반경 하향 사다리. module·tier1 모두 적용
- 결과: done (module r=1.4·tier1 r=1.4 적용, freecadcmd 검증 10항목 PASS)

## 2026-08-17 — 모듈 하단 개방 (적층 시 아래 칸과 공간 연결)
- 요청: 하단 포켓의 바닥판·앞턱을 비워 아래 모듈 상단 칸과 이어지게
- 결정: 모듈만 개방(tier1은 바닥 유지), 측면 스커트 부착용 바닥판 3mm 레일은 존치
- 결과: done (freecadcmd 재검증 9항목 PASS, 단면 점검으로 개방·레일 확인)

## 2026-08-17 — 모듈 하단을 전면 개방 수납 포켓으로 변경
- 요청: 모듈 하단(전면 밴드 + 숨은 쐐기)을 공간 활용이 되는 수납 공간으로
- 결정: 바닥판 위 평평한 포켓 + 기존 칸과 동일한 6mm 앞턱, 천장은 최하단 경사 선반 밑면. tier1도 공유 본체라 자동 반영
- 결과: done (freecadcmd 재검증 9항목 PASS, 단면 점검으로 포켓 개방 확인)
